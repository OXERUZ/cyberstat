import './globals.css';

export const metadata = {
  title: 'CYBERSTAT — Raqamli ovoz berish platformasi',
  description: 'CYBERSTAT — zamonaviy real-time raqamli ovoz berish platformasi.',
};

export default function RootLayout({ children }) {
  return <html lang="uz"><body>{children}</body></html>;
}
